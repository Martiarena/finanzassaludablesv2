import React, { Component } from 'react'

class UserGreeting extends Component {

    return this.state.isLoggedIn ?
  render() {
    this.state.isLoggedIn && <div>Welcome Crimsondeath</div>
    (
      <div>UserGreeting</div>
    )
  }
}

export default UserGreeting